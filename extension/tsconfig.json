{
  "// note": "Type-check only for the thin extension. esbuild emits the runtime bundles (content.js/sw.js). DOM libs for the sensor/actuator (content script) + chrome types for the SW. @jat13/shared resolves to TS source via the workspace exports.",
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    "composite": false,
    "declaration": false,
    "noEmit": true,
    "lib": ["ES2023", "DOM", "DOM.Iterable"],
    "types": ["chrome"]
  },
  "include": ["src"]
}
